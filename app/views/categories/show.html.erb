<div class="container">

  <div class="row">

<!-- Left Column -->
  <%= render "frames/side" %>

<!-- Main body -->

    <div class="eight columns frame main-container">

      <div class="row category-header">

        <div class="six columns">
          <h6 class="category-title"><%= @category.title.capitalize %></h6>
        </div>

        <div class="six columns category-title info--right">
          <%= link_to 'Delete category', category_path(@category.title), method: :delete, class: "menu__text" %>
        </div>

        <div class="twelve columns">

          <p class="categories-description"><%= @category.description %></p>

        </div>

      </div>

      <div class="main-container row--dark">

        <div class="row">

          <ul class="pages-list">

            <li class="card-general add-page">
              <div class="mini-card">
                <h2 class="title">Add page</h2>
                <p class="description"><%= link_to 'Create new pages', new_page_path %></p>

              </div>
            </li>
            <% if @category.pages.length > 0 %>
                <% @category.pages.each do |page| %>

                  <li class="card-general">
                    <div class="mini-card">
                      <h2 class="title"><%= link_to page.versions.last.title, page_display_path(session[:current_team]['slug'],@category.slug,page.slug) %></h2>
                      <p class="description"><%= page.versions.last.description %></p>

                      <div class="mini-card-info">
                        <p class="versions"><%= page.versions.length %> versions</p>
                        <p class="commits"><%= page.commits.length %> commits</p>
                      </div>
                    </div>
                  </li>

                <% end %>
            <% end %>
          </ul>

        </div>



      </div>



        <!-- note: Add amdin authentication here -->

      <div class="row">

        <ul class="users-list">

          <% @category.users.each do |u| %>
          <li class="user"><%= link_to u.first_char, user_path(u) %></li>
          <% end %>

        </ul>

        <% if @team_users.length > 0 %>

          <div class="new-users">

            <a href="#" id="addusers-js"><span><%= render :partial => 'svg/add' %></span></a>

            <ul class="users">

              <% @team_users.each do |user| %>
               <li> <%= link_to user.full_name, category_add_user_path(title: params[:title], user_id: user.id), method: :post %> </li>
              <% end %>

            </ul>

          </div>

        <% end %>
      </div>

    </div>
<!-- Empty Column -->

    <div class="one column empty"> Empty Column </div>


  </div>

</div>
