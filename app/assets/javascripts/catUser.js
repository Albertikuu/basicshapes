// function assignCategoryToUser(data){

// 	relationRequest = $.post("/categories", {
//     	 user_id: current_user.id,
//       	 category_id: Category.last.id + 1
//     });


//  	relationRequest.fail(console.log('the relation category request failed'));
//  	relationRequest.done(console.log('the relation category request was succesful'))

// }
